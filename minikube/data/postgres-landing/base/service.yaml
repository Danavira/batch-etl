apiVersion: v1
kind: Service
metadata:
  name: postgres-landing
  namespace: data
spec:
  selector:
    app: postgres-landing
  ports:
    - name: pg
      port: 5432
      targetPort: 5432
  type: ClusterIP