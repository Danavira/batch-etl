apiVersion: v1
kind: Service
metadata:
  name: postgres-landing-headless
  namespace: data
spec:
  clusterIP: None
  selector:
    app: postgres-landing
  ports:
    - name: pg
      port: 5432
      targetPort: 5432