apiVersion: apps/v1
kind: StatefulSet
metadata:
  name: postgres-landing
  namespace: data
spec:
  serviceName: postgres-landing-headless
  replicas: 1
  selector:
    matchLabels:
      app: postgres-landing
  template:
    metadata:
      labels:
        app: postgres-landing
    spec:
      containers:
        - name: postgres
          image: postgres:16
          ports:
            - name: pg
              containerPort: 5432
          envFrom:
            - secretRef:
                name: postgres-landing-secret
          volumeMounts:
            - name: pgdata
              mountPath: /var/lib/postgresql/data
  volumeClaimTemplates:
    - metadata:
        name: pgdata
      spec:
        accessModes: ["ReadWriteOnce"]
        resources:
          requests:
            storage: 10Gi